# My Page E2E Tests

1. @[/Users/user/Development/ai-dream-teller/.agents/PRD.md:L54] 현재까지 구현한 유저를 위한 UX Flow에 대해 프론트엔드 부분 E2E 테스트를 진행할거야
   바로 테스트를 진행하지 말고, 우선 E2E 테스트 진행을 위한 목록을 표 형태로 정리해서 PRD의 8 E2E 테스트 목록을 만들고 8.1 프론트엔드 E2E 테스트 목록을 만들어서 업데이트 해
   유저의 극단적인 행태까지 고려해서 E2E 테스트 목록을 세세하게 정리해

2. 해당 8.1 표에 E2E 테스트 완료 여부를 표시할 수 있는 새로운 칼럼을 하나 추가해

3. 이제 본격적으로 해당 목록에 따라서 E2E 테스트를 진행할거야. 첫 번째로 `기능/페이지`가 1번과 2번 `메인랜딩` 페이지의 E2E 테스트를 진행해. 특이사항이 있을 경우 8.1 표 가장 오른쪽에 `특이사항` 칼럼을 만들어서 메모해. 테스트 완료 여부를 해당 칼럼에 표시해.

4. 이제 본격적으로 해당 목록에 따라서 E2E 테스트를 진행할거야. 첫 번째로 `기능/페이지`가 1번과 2번 `메인랜딩` 페이지의 E2E 테스트를 진행해. 특이사항이 있을 경우 8.1 표 가장 오른쪽에 `특이사항` 칼럼을 만들어서 메모해. 테스트 완료 여부를 해당 칼럼에 표시해.

5. 이제 본격적으로 해당 목록에 따라서 E2E 테스트를 진행할거야. 첫 번째로 `기능/페이지`가 1번과 2번 `메인랜딩` 페이지의 E2E 테스트를 진행해. 특이사항이 있을 경우 8.1 표 가장 오른쪽에 `특이사항` 칼럼을 만들어서 메모해. 테스트 완료 여부를 해당 칼럼에 표시해.

6. `기능/페이지`가 3번부터 6번 `프로덕트상세` 페이지의 E2E 테스트를 진행해. 특이사항이 있을 경우 8.1 표 가장 오른쪽에 `특이사항` 칼럼을 만들어서 메모해. 테스트 완료 여부를 해당 칼럼에 표시해.

7. continue

8. `기능/페이지`가 7번부터 8번 `결제연동` 페이지의 E2E 테스트를 진행해. 특이사항이 있을 경우 8.1 표 가장 오른쪽에 `특이사항` 칼럼을 만들어서 메모해. 테스트 완료 여부를 해당 칼럼에 표시해.

9. `기능/페이지`가 9번부터 11번 `해석 확인` 페이지의 E2E 테스트를 진행해. 특이사항이 있을 경우 8.1 표 가장 오른쪽에 `특이사항` 칼럼을 만들어서 메모해. 테스트 완료 여부를 해당 칼럼에 표시해. sequential-thinking MCP를 활용해.

10. `기능/페이지`가 12번부터 13번 `유저마이페이지` 페이지의 E2E 테스트를 진행해. 특이사항이 있을 경우 8.1 표 가장 오른쪽에 `특이사항` 칼럼을 만들어서 메모해. 테스트 완료 여부를 해당 칼럼에 표시해. sequential-thinking MCP를 활용해.

11. `기능/페이지`가 14번과 15번 `비회원로그인` 페이지와 `비회원주문조회` 페이지의 E2E 테스트를 진행해. 특이사항이 있을 경우 8.1 표 가장 오른쪽에 `특이사항` 칼럼을 만들어서 메모해. 테스트 완료 여부를 해당 칼럼에 표시해. sequential-thinking MCP를 활용해.

12. `기능/페이지`가 16번 `지난풀이피드` 페이지의 E2E 테스트를 진행해. 특이사항이 있을 경우 8.1 표 가장 오른쪽에 `특이사항` 칼럼을 만들어서 메모해. 테스트 완료 여부를 해당 칼럼에 표시해. sequential-thinking MCP를 활용해.

13. 현재 이 대화 세션에서 이루어진 모든 대화 파일을 있는 그대로 다운로드 받고 싶어. 파일명으로는 이 대화의 제목을 달아줘. 해당 파일에서 AI의 응답은 삭제하고 유저가 입력한 프롬프트만 남겨줘. 해당 파일 내용에서 날짜와 시간으로 구분하는 내역은 모두 삭제하고 본 문서에 대해 정리 요약한 내용도 필요 없이 오직 유저의 프롬프트만 리스트로 정리해. 마크다운 저장 경로는 현재 프로젝트 내부의 `conversation_history` 안에 생성해.