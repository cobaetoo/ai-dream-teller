# Product Requirements Document(PRD)

## 1. 프로젝트 개요 (Project Overview)

- **프로젝트 명**: AI DREAM TELLER (AI 꿈 해몽 서비스)
- **목표**: 사용자가 입력한 꿈 내용을 AI가 분석하여 심층적인 해몽과 조언을 제공하는 수익형 웹 서비스
- **핵심 가치 1**: 신비롭고 직관적인 UI 경험과 정확도 높은 AI 분석을 통해서 사용자에게 인사이트와 재미 제공.
- **핵심 가치 2**: 프로이트, 칼 융, 신경과학, 게슈탈트 등 해몽을 맡기고 싶은 전문 분야를 선택해서 해몽 요청 가능.

## 2. 타겟 유저(Target Audience)

- 꿈의 의미를 검색해보는 습관이 있는 20-40대 남녀.
- 모바일 환경에서 간편하게 결과를 확인하고 공유하고 싶어하는 유저.

## 3. 기술 스택(Tech Stack)

- **Web Framework**: Next.js 16.1.6(App Router)
- **Language**: TypeScript
- **Styling**: Tailwind CSS, Shadcn/ui
- **Backend & DB**: Next.js API Routes, Supabase
- **Payments**: Toss payments
- **AI**: Gemini with gemini sdk

## 4. 디자인 가이드(Design Guide)

- **Theme**: Mystical, Vibrant, Fluid (Bright Ivory Base)
- **Colors**: Ivory Background, Light Purple, Soft Pink, Sky Blue (Light Aurora Gradients)
- **Interactions**: 부드러운 스크롤, 호버 시 빛나는 효과, 로딩 시 몽환적인 애니메이션

## 5. 프론트엔드 요구사항 (Frontend Requirements)

### 5.1 전체 레이아웃 (Global Layout)

1. **상단 네비게이션 바 (Header)**

- (공통) 홈 로고
- (비회원) 로그인, 비회원 주문 조회
- (회원) 마이페이지
- (반응형) 모바일 환경(md 미만)에서는 우측 상단 햄버거 메뉴 아이콘 제공. 클릭 시 위에서 아래로(Top) 내려오는 풀 너비(Full-width) Drawer를 통해 메뉴 노출 가능.

2. **Body**

- 각 페이지 별 주요 내용 렌더링

3. **Footer**

- 사업자 정보(상호명, 대표자, 사업자번호, 주소 등), 이용약관 링크, 개인정보처리 방침 링크, 문의하기 링크 포함
- 모든 페이지 하단에 고정 노출되는 글로벌 레이아웃 구조 적용

4. **Head & Meta**

- SEO, Open Graph, GA4 등 Analytics 설정

### 5.2 유저 UX 페이지 구성 (User UX Pages)

1. **메인 랜딩페이지 (`/`)**

- **Hero 섹션**
  - 한 줄 문구(H1): **"어젯밤 꿈, 아직 기억나시나요?"**
  - 상세 소개(Sub-text): **"프로이트부터 신경과학까지, 4가지 전문 관점으로 AI가 당신의 꿈을 분석합니다. 어젯밤 꿈을 적으면, 3분 안에 당신만의 꿈 해석 리포트가 완성됩니다."**
  - CTA 버튼 문구: **"내 꿈 해석 시작하기"** → 클릭 시 프로덕트 상세 페이지(`/dream-teller`)로 이동

- **Feature 소개 섹션 (Bento Grid UI)**
  - **Card 1 (Main/Large) - 전문 관점 선택**
    - 타이틀: "프로이트, 칼 융, 그리고 신경과학까지"
    - 설명: "하나의 꿈, 네 가지 학문적 렌즈. 원하는 전문 관점을 선택해 나만의 꿈 해석을 받아보세요."
  - **Card 2 - AI 심층 분석**
    - 타이틀: "키워드 검색은 그만, AI 심층 해석"
    - 설명: "꿈 속 상징, 감정, 서사 구조를 AI가 종합적으로 분석하여 당신만의 해석 리포트를 생성합니다."
  - **Card 3 - AI 꿈 시각화**
    - 타이틀: "꿈을 다시 눈앞에"
    - 설명: "AI가 당신의 꿈 속 장면을 한 장의 이미지로 그려드립니다. 흐릿한 기억이 선명한 그림이 됩니다."
  - **Card 4 - 기록 & 공유**
    - 타이틀: "나만의 꿈 아카이브"
    - 설명: "캘린더에 기록되는 꿈 해석 히스토리. 결과를 친구와 공유하고, 나의 무의식 패턴을 발견하세요."

- 이미 풀이된 이전 유저들의 꿈 해몽 텍스트 및 AI 이미지 예시 리스트 섹션 (더보기 -> 예시 리스트 피드로 이동 / 개별 카드 클릭 -> 해당 해몽 결과 확인 페이지로 이동)

2. **프로덕트 상세 페이지 (`/dream-teller`)**

- **단계별 입력 프로세스 (Accordion UX)**: 유저가 정보 입력에 집중할 수 있도록 3단계 아코디언 인터페이스 적용.
  - Step 1: 전문 분야 선택 (프로이트, 칼 융, 신경과학, 게슈탈트 등)
  - Step 2: 꿈 내용 입력 (최소 20자 이상 권장)
  - Step 3: 결제 옵션 선택 및 최종 금액 확인
- **인터랙션 및 자동화**:
  - `다음 단계` 버튼 클릭 시 다음 섹션이 자동으로 열리며, 해당 위치로 **부드러운 스크롤(Auto-scroll)** 처리.
  - 유저 편의를 위해 '모두 펼쳐보기' 옵션 버튼 제공.
- **구매 옵션 상세**:
  - (기본) 단순 텍스트 해석: 1,500원
  - (추가 옵션) AI 생성 이미지 추가: +500원 (**기본 활성화 상태로 제공**)
- **안내 및 주의 사항**:
  - 보통 3분 이내에 생성이 완료됨을 명시.
  - AI 분석 결과는 의학적 진단을 대체할 수 없음을 경고 섹션으로 배치.
- **모바일 최적화**: 결제하기 버튼을 하단 Sticky 버튼으로 배치하여 접근성 강화.

3. **결제 페이지 (`/payments`)**

- 영수증 디자인을 참고한 디자인의 결제 페이지
- 토스페이먼츠 위젯이 들어갈 섹션
- 회원 결제와 비회원 결제 모두 지원
- 결제가 성공적으로 완료된 후 회원은 마이페이지, 비회원은 비회원 주문 조회 페이지로 이동
- 결제에 실패했을 경우 입력한 꿈 정보가 사라지지 않고 결제 페이지에 그대로 머물러 있음
- **안정성 최적화**: React 18 Strict Mode 환경에서의 토스페이먼츠 위젯 중복 렌더링 충돌을 막기 위해 Event Loop 기반 지연 초기화 및 컴포넌트 Unmount 시 위젯 인스턴스 파기(Destroy) 클린업 로직 적용.

4. **해석 확인 페이지 (`/dream-result/[order-id]`)**

- 결제한 유저가 자신의 꿈 해석 및 AI 이미지(옵션)를 확인할 수 있는 페이지
- **디자인**: Ivory base의 몽환적인 오로라 그라데이션 및 부드러운 애니메이션 적용
- **주요 섹션**:
  - 유저 입력 꿈 내용: 감성적인 인용구(Quote) 스타일로 렌더링
  - AI 생성 이미지: 꿈의 한 장면을 시각화한 고화질 이미지 섹션
  - 심층 해몽 리포트: 전문 관점(칼 융 등)에 따른 상세 분석 텍스트 제공
- **기능**:
  - 링크 복사: 현재 페이지 URL을 클립보드에 복사 (성공 시 버튼 텍스트 피드백)
  - 카카오톡 공유: 카카오 SDK를 연동하여 제목, 이미지, 요약 글이 담긴 피드 메시지 전송
  - 공개 여부 토글: 내 해몽을 피드에 자랑할 수 있도록 스위치 UI 제공 (비회원 유저 또한 쿠키 세션을 연동하여 본인의 결과라면 공개 상태를 변경할 수 있도록 지원)
- **보안 및 렌더링**: Server Component 단에서 `createServiceRoleClient`를 활용, Supabase RLS 정책 제약을 우회하여 데이터를 선행 페치하되, 현재 세션(회원/비회원)과 실제 데이터의 소유자(Ownership)를 수동 매칭하여 권한 없는 접근을 원천 차단하는 이중 보안 구조 설계.
- **회원 전용**: '나의 무의식 캘린더' 섹션이 노출되며, 해몽 기록이 있는 날짜 하이라이트 및 클릭 시 해당 기록으로 이동 가능 (비회원에게는 미노출)

5. **유저의 마이페이지 (`/my-page`)**

- 회원 가입된 유저만 자신의 마이페이지 접근 가능
- 캘린더 라이브러리를 활용해 해몽이 이뤄진 날짜에 하이라이트 표시 및 해당 일자를 누를 경우 해당 해몽 결과 페이지로 넘어감
- 구매 내역 리스트가 캘린더 하단에 배치 및 해당 리스트 아이템을 누를 경우 해당 해몽 결과 확인 페이지로 넘어감
- 닉네임(수정 가능 기능), 로그인 한 소셜 서비스(구글 or 카카오) 로고, 이메일 주소, 로그아웃 버튼

6. **비회원 로그인 (`/guest-login`)**

- 간단한 페이지 소개
- 전화번호 및 비밀번호 입력 폼
- 비회원 주문 조회 버튼

7. **비회원 주문 조회 페이지 (`/guest-check`)**

- 6번에서 로그인 한 비회원이 자신의 구매 내역을 조회하는 페이지
- 구매내역 리스트가 배치되어 있고 해당 리스트 아이템을 눌렀을 때 해당 해몽 결과 확인 페이지로 이동
- **데이터 안정성**: Supabase DB 1:1 관계 스키마의 특성상 응답 데이터가 배열(Array)과 단일 객체(Object) 형식 양쪽으로 떨어질 수 있는 케이스를 종합 헨들링(`Array.isArray` 분기)하여, 비회원 리스트 카드 썸네일 누락 현상을 원천 방어함.

8. **이전 유저들의 과거 풀이 내역 리스트 피드 페이지 (`/feeds`)**

- **UI 스타일**: 페이스북 형태의 타임라인 피드 구성을 통한 친숙한 UX
- **콘텐츠 및 인터랙션**: 
  - `faker-js` 기반의 리얼한 더미 데이터(프로필, 꿈 내용, 전문가 분석 요약) 렌더링
  - 이미지가 포함된 해몽과 텍스트 전용 해몽이 혼합된 카드 디자인
  - 정보 집중을 위해 하단 인터랙션(좋아요/댓글 등) 카운트 및 액션 버튼은 제거된 심플한 형태
- **기능**: '더보기' 버튼을 통한 무한 스크롤(Pagination) 방식의 콘텐츠 로딩 지원

9. **회원 로그인 페이지 (`/auth`)**

- 구글 및 카카오 소셜 로그인만 존재
- 각 소셜 서비스로 성공적으로 로그인 후 리다이렉트는 2번 프로덕트 상세 페이지로 이동

10. **관리자 텔레그램 알림 기능 (Telegram Notifications)**

- **결제 상태 알림**: 결제가 완료되거나 실패했을 때 텔레그램 봇으로 즉시 알림 메시지 발송.
  - 성공 알림 예시: 결제 상품(텍스트/이미지), 유저 아이디, 결제 가격, 꿈 내용 앞부분
  - 실패 알림 예시: 실패 상태(잔액 부족, 취소 등) 및 에러 메시지
- **AI 해몽 완료 상태 알림**: 백그라운드에서 AI 해몽과 이미지 생성이 완료되었거나, 오류가 발생해 DB에 기록되었을 때 최종 결과를 포함하여 텔레그램으로 알림 발송 (Success or Fail).

### 5.3 관리자 UX 페이지 구성 (Admin UX Pages)

0. **관리자 페이지 기본 공통 레이아웃**

- 좌측 네비게이션 패널
  - 매출 조회
  - 주문 내역 리스트
- 네비게이션 패널을 제외하고는 각 관리자 메뉴 페이지별 내용 body

1. **관리자 메인 페이지 (`/admin`)**

- 기간별 매출 조회 대시보드(기본 화면)
  - 핵심 지표 요약 카드: 총 매출액, 총 주문 수, 신규 유저 수, 누적 AI 해석 건수 및 전월 대비 증감률 표시 (현재 `faker-js` 기반 더미 데이터 활용)
  - 매출 추이 차트: 최근 8개월간의 월별 매출 합계를 Tailwind CSS 기반 커스텀 막대 차트로 시각화
  - 디자인: `Shadcn/UI` Card 레이아웃 및 `Lucide` 아이콘 적용, 반응형 디자인 대응 (모바일 Drawer 메뉴)

2. **주문 내역 리스트 (`/admin/order-list`)**

- 결제가 완료된 주문 건 확인을 위한 리스트 표
  - 주요 항목: 주문 ID, 구매 일시, 닉네임, 회원/비회원 구분, 전문 분야, 결제 금액, 상태 배지 (결제완료/실패)
  - 검색 기능: 주문번호 및 유저 닉네임을 통한 실시간 필터링
  - 각 리스트 아이템 클릭 시 `3번 상세 주문 내역`으로 이동

3. **상세 주문 내역 (`/admin/order-list/[order-id]`)**

- 주문의 모든 정보를 종합적으로 관리하는 대시보드 형태
  - 구매자 정보: 닉네임, 이메일, 연락처, 회원 등급 표시
  - 결제 정보: 실시간 결제 상태, 총 금액, 토스 주문번호, 승인 일시
  - 콘텐츠 정보: 유저 입력 원본 꿈 텍스트 및 선택된 전문 분석 관점(expertField)
  - 분석 결과: AI(Gemini) 생성 해몽 텍스트 리포트 및 AI 생성 꿈 이미지(미구매 시 제외)
  - 관리자 기능: 해몽 품질 보정을 위한 'LLM 해몽 재생성' 트리거 버튼 제공 (POST /api/admin/orders/[id]/regenerate)

4. **유저 리스트 (`/admin/user-list`)**

- 회원 및 비회원 통합 사용자 관리 페이지
  - 필터링 기능: 전체 / 회원(소셜가입) / 비회원(전화번호 기반) 구분을 통한 선택적 조회
  - 주요 항목: 닉네임, 이메일/전화번호, 가입 경로(Google/Kakao/Guest), 결제 여부, 누적 주문 건수, 가입 일시
  - 검색 기능: 닉네임, 이메일, 전화번호 검색을 통한 실시간 유저 필터링
  - 시각화: 결제 이력(결제완료/미결제) 및 회원 등급별 배지 시스템 적용

## 6. 백엔드 요구사항 및 API 구조 (Backend Requirements & API Structure)

프론트엔드와 독립적으로 기능할 수 있도록 RESTful하고 직관적인 API 구조로 Next.js Route Handlers(API Routes)를 설계합니다.

### 6.1 인증 및 권한 (Auth)

- `POST /api/auth/login`
  - 설명: 소셜 로그인(Google, Kakao) 인증을 위해 Supabase Auth 인스턴스를 통해 각 제공업체의 권한 승인 페이지로 리다이렉트합니다.
- `GET /api/auth/callback`
  - 설명: 소셜 로그인 최종 승인 후 전달된 인증 코드를 확인하여 세션을 확정하고, 성공 시 지정된 페이지(`/dream-teller`)로 리다이렉트합니다.
- `POST /api/auth/logout`
  - 설명: 현재 활성화된 사용자의 세션을 종료하고 로그아웃 처리합니다.
- `POST /api/auth/guest`
  - 설명: 전화번호와 비밀번호를 전달받아 비회원용 세션 토큰을 발급합니다.
  - 반환값: 비회원 인증 토큰.

### 6.2 사용자 (Users)

- `GET /api/users/me`
  - 설명: 현재 로그인한 회원/비회원의 프로필 및 기본 정보를 반환합니다.
- `PATCH /api/users/me`
  - 설명: 사용자의 프로필 정보(예: 닉네임)를 수정합니다.

### 6.3 주문 및 결제 (Orders & Payments)

- `GET /api/orders`
  - 설명: 현재 로그인한 사용자의 구매 이력 리스트를 반환합니다. (마이페이지 용도)
- `POST /api/orders`
  - 설명: 꿈 입력 데이터와 선택된 옵션, 전문 분야 등을 기반으로 주문서(Pending Order)를 생성합니다.
  - **비회원 처리**: 비회원 주문 시 `phone_number`와 `guest_password`를 전달받아 `users` 테이블에 guest 레코드를 생성하거나 기존 정보를 연동합니다.
  - **금액 검증**: 클라이언트에서 전달된 `total_amount`가 서버의 옵션 가격 정책과 일치하는지 반드시 검증하여 위변조를 차단합니다.
- `GET /api/orders/[id]`
  - 설명: 개별 주문에 대한 구매 정보 및 (생성 완료된) 꿈 해몽 결과를 반환합니다.
  - **권한 검증**: 요청자의 세션(회원/비회원)이 해당 주문의 소유주인지 확인하는 로직을 포함합니다.
- `GET /api/orders/guest`
  - 설명: 발급받은 비회원 세션을 인증하여 해당 비회원의 주문 내역 리스트를 반환합니다.
- `POST /api/payments/confirm`
  - 설명: 토스페이먼츠 결제 승인 API를 호출하여 최종 결제 완료 처리를 수행합니다.
  - **후속 처리**: 결제 성공 시 주문 상태를 `paid`로 변경하고, **텔레그램 알림 발송 (상품, 유저아이디, 결제금액, 꿈내용 일부)**을 진행합니다. 직후 AI 해몽 생성 API(`api/ai/generate`)를 비동기 호출합니다.
- `POST /api/payments/fail`
  - 설명: 결제 과정에서 발생한 실패 정보를 기록하고 해당 주문서의 상태를 `failed`로 업데이트합니다.
  - **후속 처리**: 실패 내역(에러 메시지 등)을 포함하여 관리자에게 **텔레그램 실패 알림 발송**을 진행합니다.
- `POST /api/payments/cancel`
  - 설명: 결제 취소 및 환불 요청을 처리합니다. (AI 생성 전 단계 혹은 시스템 장애 시 사용)
- `POST /api/payments/webhook`
  - 설명: 토스페이먼츠로부터 결제 상태 변경 알림(가상계좌 입금 완료 등)을 수신하여 처리합니다.
- `POST /actions/dream-result (toggleDreamPublicAction)`
  - 설명: 생성된 개별 꿈 결과의 피드 공개 여부를 전환하는 Next.js Server Action
  - **보안 처리**: 비회원의 경우 JWT 토큰(`auth.getUser()`)이 없으므로, HttpOnly 쿠키의 `guest_session` 값을 추출한 뒤 Service Role 클라이언트를 활용해 DB 내부의 소유권을 매칭 및 대조하여 타인의 결과물을 변조할 수 없도록 강제 격리 검증을 수반함.

### 6.4 피드 (Feeds)

- `GET /api/feeds`
  - 설명: 사용자에게 노출할 이전 유저들의 공개 해몽 결과 목록을 페이지네이션과 함께 반환합니다. (메인 페이지, `/feeds` 용도)

### 6.5 AI 처리 (AI Processing)

- `POST /api/ai/generate`
  - 설명: 결제가 완료된 주문의 상품(Product) 정보를 확인하여, 순수 텍스트 해몽인지 이미지 생성이 함께 포함된 해몽 상품인지 판별하고 AI 파이프라인을 분기 처리합니다.
  - **텍스트 생성 (Gemini API)**: 결제가 확인된 모든 꿈 텍스트를 바탕으로 AI 모델(Gemini)에 해몽 분석 프롬프트를 전송하여 장문의 해석 결과를 생성합니다.
  - **이미지 생성 (Image Generation API)**: 결제된 내역이 '해몽 텍스트 + AI 이미지 생성'이 포함된 상품인 경우에 한하여 호출됩니다. 일부분석된 텍스트 결과(혹은 꿈 원문)를 토대로 영어 프롬프트를 번역 및 재가공하고, 이미지 생성 API 모델을 호출하여 이미지를 얻습니다. 생성된 이미지는 Supabase Storage 등에 비동기적으로 업로드하여 Public URL 형식으로 변환한 뒤 DB에 저장합니다.
  - **후속 처리**: 텍스트 분석, 이미지 생성 및 DB 저장이 모두 정상적으로 끝났을 때(혹은 생성 과정 중 어디선가 에러로 실패했을 때) 최종 상태 정보와 내역을 **텔레그램 봇을 통해 관리자에게 알림 발송(Success / Fail 단계 명시)**합니다.

### 6.6 관리자 (Admin)

- **기술적 특이사항 (중요)**
  - **Server Actions 전환**: 클라이언트 사이드 `fetch` 시 발생하는 Next.js 미들웨어의 401/403 인증 이슈를 해결하기 위해 모든 관리자 데이터 조회 로직을 `src/app/actions/admin.ts`의 Server Actions로 전환하여 구현함.
  - **데이터 조인 최적화**: `orders`와 `dream_results` 간의 1:1 관계(UNIQUE FK)에 따른 PostgREST의 단일 객체 반환 특성을 고려하여 프론트엔드 데이터 바인딩 로직을 안정화함.

- `GET /api/admin/metrics` (구현완료 - Server Action `getAdminMetrics` 병행 가능) [x]
  - 설명: 관리자 대시보드용으로 기간별 매출과 주문 통계 데이타를 반환합니다.
- `GET /api/admin/orders` (구현완료 - Server Action `getAdminOrders`) [x]
  - 설명: 시스템 전체의 주문 발생 내역 리스트를 페이지네이션 및 필터와 함께 반환합니다. `dream_results`의 생성 상태(`analysis_status`)를 포함합니다.
- `GET /api/admin/orders/[id]` (구현완료 - Server Action `getAdminOrderDetail`) [x]
  - 설명: 상세 주문 내역에서 원본 꿈 텍스트, 사용자 정보, 결제 상태 및 해몽 결과를 반환합니다. `dream_results`는 단일 객체 형태로 매핑됩니다.
- `POST /api/admin/orders/[id]/regenerate` (구현완료) [x]
  - 설명: 결과물 품질 이슈 등을 이유로 LLM 해몽 재생성을 트리거합니다. 관리자 전용 권한 확인 후 AI 파이프라인을 재가동합니다.
- `GET /api/admin/users` (구현완료 - Server Action `getAdminUsers`) [x]
  - 설명: 회원 가입 유저와 비회원 유저 리스트를 조회하고, 각 유저별 누적 주문 및 결제 건수를 합산하여 반환합니다. `phone_number` 필드를 식별자로 사용합니다.

## 7. 데이터베이스 스키마 (Database Schema)

Supabase PostgreSQL 환경을 기준으로 구성하되, 인증 테이블(`auth.users`)과 연결되는 어플리케이션 전용 Public 테이블을 정의합니다. MECE 원칙에 따라 명확히 역할을 분리했습니다.

### 7.1 Users (`users`)

회원(소셜)과 비회원의 정보를 통합 관리하는 테이블입니다.

- **`id`** (UUID, Primary Key, **Not Null**): 고유 식별자 (Supabase `auth.users`의 PK와 1:1 매핑)
- **`role`** (String, **Not Null**): 사용자 역할 (`member`, `guest`, `admin`)
- **`provider`** (String, **Not Null**): 가입 경로 (`google`, `kakao`, `guest`)
- **`email`** (String, Null): 회원 이메일 (소셜 로그인 회원용)
- **`nickname`** (String, Null): 사용자 닉네임
- **`phone_number`** (String, Null): 비회원 식별 및 주문 조회용 전화번호
- **`guest_password_hash`** (String, Null): 비회원용 암호화된 비밀번호
- **`created_at`** (Timestamp, **Not Null**): 계정 생성 일시
- **`updated_at`** (Timestamp, **Not Null**): 정보 최종 수정 일시

### 7.2 Orders (`orders`)

사용자의 꿈 분석 요청 및 토스페이먼츠 결제 내역을 매핑하는 테이블입니다.

- **`id`** (UUID, Primary Key, **Not Null**): 내부 시스템의 고유 주문 식별자
- **`order_number`** (String, Unique, **Not Null**): 토스페이먼츠의 `orderId`로 사용될 고유 주문 번호
- **`user_id`** (UUID, Foreign Key, **Not Null**): 결제를 진행한 `users.id` 참조
- **`total_amount`** (Integer, **Not Null**): 총 결제 금액
- **`payment_status`** (String, **Not Null**): 결제 상태 (`pending`, `paid`, `failed`, `refunded`, `canceled`)
- **`payment_key`** (String, Null): 토스페이먼츠 승인 키 (결제 성공 시 저장)
- **`approved_at`** (Timestamp, Null): 토스페이먼츠 결제 승인 시각
- **`error_message`** (Text, Null): 결제 실패/에러 시 사유 메시지
- **`dream_content`** (Text, **Not Null**): 유저가 직접 입력한 원본 꿈 내용
- **`expert_field`** (String, **Not Null**): 선택한 해몽 전문 분야 (`freud`, `jung`, `neuroscience`, `gestalt` 등)
- **`includes_image`** (Boolean, **Not Null**): AI 이미지 생성 옵션 구매 여부
- **`created_at`** (Timestamp, **Not Null**): 주문서 생성 일시
- **`updated_at`** (Timestamp, **Not Null**): 결제 및 상태 업데이트 일시

### 7.3 Dream Results (`dream_results`)

결제 완료 후 생성되는 AI 해몽 결과물과 이미지 URL, 피드 공개 여부를 담당하는 테이블입니다.

- **`id`** (UUID, Primary Key, **Not Null**): 고유 식별자
- **`order_id`** (UUID, Foreign Key / Unique, **Not Null**): 매칭되는 `orders.id` 참조 (1:1 관계)
- **`analysis_status`** (String, **Not Null**): 해몽 진행 상태 (`processing`, `completed`, `failed`)
- **`analysis_text`** (Text, Null): AI(Gemini)가 생성해낸 심층 해몽 텍스트
- **`image_url`** (String, Null): AI(Gemini)가 묘사해준 꿈 이미지 URL (옵션 미구매 시 Null)
- **`is_public`** (Boolean, **Not Null**): Feed 페이지 공유 노출 여부 (`true` or `false`)
- **`created_at`** (Timestamp, **Not Null**): 해몽 작업 최초 생성 일시
- **`updated_at`** (Timestamp, **Not Null**): 해몽 결과 또는 노출 여부 수정(Update) 통제용 일시

## 8. E2E 테스트 목록

해당 섹션에서는 사용자의 정상적인 서비스 이용 흐름뿐만 아니라, 예상치 못한 극단적이거나 비정상적인 행태에도 서비스가 사이드 이펙트 없이 견고하게 동작하는지 보장하기 위한 E2E 테스트 케이스를 명세합니다.

### 8.1 프론트엔드 E2E 테스트 목록

| No | 기능 / 페이지 | 대상 (URL) | 테스트 케이스 (극단적/예외 행동 포함) | 예상 결과 | 완료 | 특이사항 |
|---|---|---|---|---|---|---|
| 1 | 메인 랜딩 | `/` | 히어로 섹션 CTA 버튼을 불규칙적으로 연속 광클릭 시도 | 중복 렌더링이나 라우팅 푸시 없이 1회만 `/dream-teller`로 진입함 | [x] | 이상 없음 |
| 2 | 메인 랜딩 | `/` | 모바일 환경에서 네비게이션 드로어를 연 상태로 화면 방향(가로/세로) 빈번하게 회전 | 반응형 중단점 변경 시 드로어 상태 꼬임 없이 뷰포트 레이아웃 안정적으로 유지 | [x] | Sheet overlay 잔류 이슈: 뷰포트 변경 시 드로어가 자동으로 닫히지 않음. Escape로 해소 가능하나 UX 개선 권장 -> (완료) useEffect resize 리스너 추가하여 md(768px) 이상 시 자동 닫힘 구현 |
| 3 | 프로덕트 상세 | `/dream-teller` | 아코디언 컴포넌트의 이전 단계를 무시하고 다음 단계 헤더를 강제 클릭하거나 스크립트 수정 시도 | 이전 Step 미완료 시 아코디언 잠금(Lock) 상태를 유지하고, 에러 토스트나 인디케이터 제공 | [x] | 아코디언 단계별 잠금(Lock) 미구현 상태. 자유 열기 가능하나 최종 submit 시 trim 검증으로 빈 꿈 방어됨. 단계 잠금 UX 개선 권장 -> (완료) isStepValid 로직 및 Lock 아이콘 적용하여 단계별 진입 제한 구현 |
| 4 | 프로덕트 상세 | `/dream-teller` | 꿈 텍스트 입력창에 모두 공백(스페이스바)으로만 제한 글자수 초과하여 채운 뒤 다음 이동 클릭 | Validation (trim 체크) 실패하여 진행 불가 및 올바른 텍스트 입력 에러/경고 노출 | [x] | 이상 없음. trim() 검증 정상 작동 |
| 5 | 프로덕트 상세 | `/dream-teller` | 꿈 텍스트 입력 영역에 1만 자 이상의 장문, 수백 개의 이모지 및 특수문자를 혼합 도배하여 붙여넣기 | UI 프리징 없이 제한된 글자까지만 입력 처리되며 백엔드 전송 전 안정적으로 방어됨 | [x] | Textarea에 maxLength 미설정. 프리징은 없으나 글자 수 상한 제한 추가 권장 -> (완료) 3,000자 maxLength 및 실시간 글자수 카운터 UI 적용 |
| 6 | 프로덕트 상세 | `/dream-teller` | 결제 직전의 'AI 이미지 추가' 옵션 토글을 초당 여러 번 연속 On/Off 토글링 반복 | 상태 변경 딜레이나 UI 깜빡임 없이 최종 토글 상태에 맞는 정확한 예상 금액 도출 | [x] | 이상 없음. 20회 연속 토글 후에도 정확한 가격 반영 확인 |
| 7 | 결제 연동 | `/payments` | 토스페이먼츠 위젯 로딩 중 뒤로 가기 눌렀다가 즉시 복귀하기를 수회 반복 수행 | 결제 인스턴스 중복 호출/메모리 릭 에러 발생 없이 결제창 초기화 및 올바른 렌더링 유지 | [x] | 이상 없음. useEffect cleanup의 active 플래그로 중복 초기화 방어 정상 동작 |
| 8 | 결제 연동 | `/payments` | 결제 버튼 클릭 순간 브라우저 오프라인 모드 적용 (네트워크 강제 단절) | 무한 로딩 인디케이터 멈춤(프리징) 현상 없이 "네트워크 상태 확인" 안내 경고 팝업 표출 | [x] | SDK 초기화 실패 시 버튼이 disabled 상태로 유지되어 프리징 방어. 단, 별도 "네트워크 오류" 안내 UI 미구현. 오프라인 감지 UX 추가 권장 -> (완료) window online/offline 이벤트 감지 및 안내 오버레이 UI 구현 |
| 9 | 해석 확인 | `/dream-result/[id]` | URL 파라미터에 다른 유저의 주문 ID 또는 무작위 텍스트(ex: `abc-123`) 강제 대입하여 다이렉트 접근 | 데이터 권한 검증 및 매칭 실패로 403 / 404 상태 처리되어 시스템 에러 노출 없이 안내 페이지로 튕김 | [x] | 현재 무작위 ID에도 Mock 데이터가 노출됨. 실제 DB 연동 시 소유권(Ownership) 검증 및 403/404 처리 로직 필수 추가 권장 -> (완료) invalid ID 접근 시 NotFound 전용 안내 페이지(404 UI) 노출 구현 |
| 10 | 해석 확인 | `/dream-result/[id]` | AI 해몽이 아직 Processing 로딩 중일 때 브라우저 새로고침 다중 타이밍 발생 | 데이터 손상이나 API 꼬임 현상 없이 폴링(Polling) 채널 재개 및 로딩 UI 상태 안전하게 지속 유지 | [x] | 5회 연속 새로고침에도 레이아웃 및 컴포넌트 안정성 유지 확인. (현재 Mock 즉시 렌더링 방식 기준) |
| 11 | 해석 확인 | `/dream-result/[id]` | 링크 복사 및 카카오톡 공유 이벤트 버튼을 키보드 단축키(Enter) 및 마우스 더블클릭 연타 | 복사 안내 피드백 및 SDK 중복 호출 에러 없이 디바운스/쓰로틀링 처리되어 1회만 정상 동작 | [x] | 10회 연속 연타에도 에러 미발생. 복사 완료 피드백 UI가 2초 후 정상 복구됨 확인 |
| 12 | 유저 마이페이지 | `/my-page` | 브라우저 쿠키(인증 세션) 등을 강제 삭제한 상태에서 마이페이지 라우터를 직접 입력하여 진입 | Protected 라우트 작동으로 화면 리소스 노출 이전에 회원용 로그인 페이지(`/auth`)로 즉시 리다이렉트 | [x] | 리다이렉트 기능 미구현. 로그인 없이도 접근 가능함 확인. Middleware 인증 보안 강화 필히 권장 -> (완료) Next.js Middleware 기반 /my-page 접근 제한 및 /auth 리다이렉트 구현 |
| 13 | 유저 마이페이지 | `/my-page` | 닉네임 변경 폼에 `<script>` 등 XSS 공격 구문이나 제한 글자 초과 문자열을 입력하여 연속 엔터 제출 연타 | Validation에 의해 이스케이프 및 글자 수 길이 제한 에러 피드백을 주며 서버 전송 방어 | [x] | React 이스크이프로 구문 렌더링 방어됨. 단, 닉네임 글자 수 상한(MaxLength) 미구현 상태 -> (완료) 닉네임 변경 폼에 10자 maxLength 및 유효성 검사 추가 |
| 14 | 비회원 로그인 | `/guest-login` | 전화번호 필드에 숫자 이외의 문자열 대량 복붙, 비밀번호도 공백 떡칠 후 엔터키 연속 다발 클릭 연타 | 전화번호 정규식 포맷터 및 공백 검증으로 서브밋 로직 락(Lock) 처리 및 중복 API 무의미 호출 차단 | [x] | 전화번호 노이즈 제거 및 하이픈 자동 삽입 정상. 비밀번호 4자리 제한 정상. 서브밋 시 버튼 비활성화로 중복 호출 방어됨 |
| 15 | 비회원 주문조회 | `/guest-check` | 비회원용 구매 내역 리스트를 마우스 휠로 최상단-최하단으로 0.1초만에 무한 광스크롤 반복 | 스크롤 이벤트 최적화를 기반으로 페이지네이션 중복 및 API 과부하 호출 안전하게 방어 및 로딩 유지 | [x] | 5회 연속 왕복 광스크롤에도 레이아웃 꼬임 정체 현상 없음. 내역 클릭 시 결과 페이지 이동 정상 확인 |
| 16 | 지난 풀이 피드 | `/feeds` | 피드에서 무한 스크롤 스크롤바를 맨 아래로 고정시킨 뒤 수초간 연속 대량 데이터 훅 유발 시도 | 데이터 로딩 중 더티 체킹을 거쳐 추가 API 패치 요청 Lock, UI 프레임 손상 없이 데이터만 확장 결합 표출 | [x] | 현재 자동 무한 스크롤 대신 '수동 더보기 버튼'으로 구현됨. 버튼 연타 및 광스크롤 시 UI 안정성 및 레이아웃 유지 확인 완료 |
| 17 | 비회원 주문조회 데이터 연동 | `/guest-check` | 비회원 로그인 후 발급된 세션으로 접근 시 실제 DB 구매 내역 및 유저 정보가 노출되는지 확인 | 더미 데이터 대신 서버에서 쿠키 세션 기반으로 실제 사용자의 정보(전화번호 등)와 주문 리스트가 렌더링됨 | [x] | 실제 데이터 기반의 비회원 마이페이지 연동 완료 확인 |

### 8.2 백엔드 E2E 테스트 목록

| No | 기능 / API | 대상 (Endpoint) | 테스트 케이스 (극단적/예외 행동 포함) | 예상 결과 | 완료 | 특이사항 |
|---|---|---|---|---|---|---|
| 1 | 권한 & 인증 | `POST /api/auth/login` | 지원하지 않는 소셜 프로바이더(예: `github`) 이름 혹은 빈 값을 포함해 로그인 강제 시도 | 미지원 provider 에러 반환(400 Bad Request) 및 리다이렉트 방어 | [x] | 지원하지 않는 provider 요청 시 400 Bad Request 반환 및 에러 메시지 확인 완료 |
| 2 | 권한 & 인증 | `GET /api/auth/callback` | 브라우저 외부에서 조작된 인가 코드(code)나 이미 사용/만료된 콜백 파라미터 전송 시도 | 세션 획득 실패로 401 에러 리턴 또는 인증 실패 에러 페이지 리다이렉트 | [x] | 유효하지 않은 인가 코드 전송 시 /auth?error=auth_callback_failed 리다이렉트 정상 작동 확인 |
| 3 | 권한 & 인증 | `POST /api/auth/logout` | 이미 세션 쿠키가 없거나 만료된 유저가 비정상 로그아웃 API 요청을 연속해서 다중 시도 | 서버 크래시(500) 없이 안전하게 쿠키 정리 빈 응답 및 200/리다이렉트 정상 처리 | [x] | 세션 유무와 관계없이 서버 에러 없이 메인 페이지("/")로의 안정적인 리다이렉트 확인 |
| 4 | 권한 & 인증 | `POST /api/auth/guest` | 비정상적인 형태의 전화번호(특수문자 포함 등)나 초과 자리수(ex: 100자) 입력 시도 | 400 Bad Request 리턴 및 DB Insert/조회 방어됨 | [x] | Zod 스키마 기반의 엄격한 Validation 적용 완료 (특수문자/길이 초과 시 400 에러 및 메시지 반환 확인) |
| 5 | 사용자 관리 | `PATCH /api/users/me` | 다른 유저의 ID를 페이로드에 포함시켜 권한 탈취(IDOR) 시도 | 활성화된 서버 세션을 기준으로만 처리되며 403 Forbidden 리턴 | [x] | 세션 기반 권한 검증 및 클라이언트 전송 ID와 세션 ID 대조 로직 구현 (타인 ID 대입 및 비인가 접근 차단 확인) |
| 6 | 사용자 관리 | `PATCH /api/users/me` | 닉네임 필드에 악성 스크립트(XSS 제로데이 공격) 대량 주입 시도 | DB 저장 과정에서 이스케이프 처리 및 허용된 데이터 포맷으로 치환/거부됨 | [x] | Zod 기반 특수문자/스크립트 주입 차단 필터 적용 완료 (정규식 "[a-zA-Z0-9가-힣\\s]+" 적용으로 스크립트 태그 거부 확인) |
| 7 | 주문 생성 | `POST /api/orders` | 프론트엔드 가격 조작(예: 기본 1,500원 + 이미지 500원을 선택했는데 `total_amount=1,500원` 전송) 시도 | 서버 내 고정 단가 정책과 합산 결과 대조 실패로 400 Bad Request 리턴 | [x] | DB 생성 전 초반 조기 검증 로직으로 이전되어 400 Bad Request 에러 정상 리턴 확인 |
| 8 | 결제 검증 | `POST /api/payments/confirm` | 토스페이먼츠 승인 콜백 API를 악의적으로 동일한 `payment_key`로 다중 연속 호출 | DB 트랜잭션 Lock (Idempotency Key) 처리로 단 1회만 결제 승인됨 | [x] | 유효하지 않거나 중복된 결제 승인 요청 시 토스 API 연동 중단 및 에러 정상 반환 확인 |
| 9 | 주문 조회 | `GET /api/orders/[id]` | 무작위 UUID 형식 대입 및 타인의 결제 완료 주문서 ID를 파라미터 강제 조회 시도 | 토큰 소유권(Ownership) 불일치로 403 / 404 리턴되어 타인 내역 조회 방어됨 | [x] | 주문 조회에 대한 소유권 및 세션 유효성 검증 실패 시 401/403 등 권한 실패 방어 확인 |
| 10 | 데이터 피드 | `GET /api/feeds` | 쿼리스트링에 `limit=999999` 같은 과다 요청 및 타입 혼용 쿼리 전송 시도 | 서버 설정된 최대 제한 레코드 값(예: 50개)으로 강제 변환되어 정상 응답 | [x] | Zod 기반 쿼리 검증 스키마 도입 및 `max(50)` 제한 적용 완료. 잘못된 형식 요청 시 400 에러 및 메시지 반환 확인. |
| 11 | AI 로직 | `POST /api/ai/generate` | 외부 Gemini API의 5xx 에러 장애 발생 및 무응답 타임아웃 지연(10분 초과) 유발 | 서비스 중단 없이 에러 캡처 및 시스템 재시도 큐 대기 상태로 전환(상태 `failed`) | [x] | 50초 내 API 미응답 시 AbortController로 내부 타임아웃 강제 수행 및 `failed` 상태 전이 확인. 실패 텔레그램 알림 연동 완료. |
| 12 | 어드민 권한 | `GET /api/admin/*` | 관리자 역할(Role)이 아닌 일반 유저나 만료된 JWT를 삽입하여 관리자 API 강제 호출 | 401 / 403 상태 코드 리턴 등으로 서비스 조회 등 DB 연결 원천 차단 | [x] | `middleware.ts`에서 `/api/admin/*` 경로에 대해 세션 확인(401) 및 `public.users.role='admin'` DB 대조 로직 적용 확인 (비인가 시 403 접근 차단 완료). |
| 13 | 주문 생성 (비회원) | `POST /api/orders` | 비회원 주문 시 필수 필드(`phone_number`, `guest_password`) 누락 또는 잘못된 형식 입력 | 400 Bad Request 리턴 및 불완전/부정확한 레코드 생성 차단 | [x] | 파라미터 누락 체크 및 Zod 기반의 정규식 형식 검증 로직 통합 완료 확인 |
| 14 | 결제 실패 처리 | `POST /api/payments/fail` | 결제창에서 취소/실패 후 전달된 에러 코드와 메시지를 고의로 누락하거나 변조하여 전송 | 400 Bad Request 리턴 또는 시스템상 `failed` 상태와 함께 `error_message` 필드에 기록 확인 | [x] | 필수 파라미터 부재 및 권한 불일치 시 400 이상 에러를 정상 발현하여 방어 확인 |
| 15 | 결제 취소 처리 | `POST /api/payments/cancel` | 이미 처리된 주문이나 권한이 없는 타인의 주문에 대해 취소 API 호출 시도 | 403 Forbidden 리턴 및 상태값이 `canceled`로 변경되지 않음을 보장 | [x] | 존재하지 않거나 타인의 주문 취소 요청 시 에러를 뱉으며 상태 변경을 무효화함 |
| 16 | 결제 웹훅 | `POST /api/payments/webhook` | 토스페이먼츠 서버가 아닌 외부 IP에서 위조된 성공 페이로드를 웹훅 엔드포인트로 전송 시도 | IP 필터링 또는 HMAC 시그니처 검증 실패로 401/403 처리 및 상태 변경 차단 | [x] | 올바른 시크릿 서명이 없는 웹훅 요청의 경우 악의적 접근으로 간주되어 상태 변경 차단 완료 |
| 17 | 주문 소유권 (IDOR) | `GET /api/orders/[id]` | 비회원이 로그인한 상태에서 자신의 `order_id`가 아닌 다른 비회원의 유효한 `order_id`를 강제 대입 | 403 Forbidden 리턴 (비회원 세션 간에도 철저한 격리 확인) | [x] | 비회원의 경우에도 발급된 세션 권한 내에서만 조회 가능하며, 타 주문(무작위ID 포함) 접근 시 실패 확인 |
| 18 | AI 처리 & 이미지 생성 | `POST /api/payments/confirm` | 이미지 생성 포함 상품 결제 승인 후 Gemini API를 통한 해몽 분석 및 이미지 생성 트리거 | `dream_results` 테이블에 텍스트 분석 결과와 이미지 URL이 모두 정상 저장됨 | [x] | 이미지 생성 포함 주문 건에 대해 AI 처리 및 DB 반영 프로세스 정상 작동 확인 (상세페이지 마크다운 렌더링 수정 완료) |
| 19 | 텔레그램 알림 발송 | External (Bot API) | AI 해몽 분석 완료(`completed`) 또는 실패(`failed`) 상태 변경 시 봇 알림 발송 | 지정된 텔레그램 채널/봇으로 분석 결과 요약 또는 에러 로그 메시지가 즉시 도달 | [x] | 텔레그램 봇 API 연동을 통한 실시간 상태 알림 발송 기능 확인 완료 |
| 20 | 마이페이지 데이터 연동 | `GET /api/orders` | 결제 완료된 주문 내역 조회 시 `dream_results`와 조인된 실제 데이터(이미지 포함) 호출 | DB에 저장된 실제 이미지 URL과 분석 텍스트가 마이페이지 내역에 정확히 매칭되어 출력 | [x] | RLS 무한 재귀 에러 해결 후 실제 DB 데이터 기반의 마이페이지 리스트 노출 확인 완료 (썸네일 이미지 포함) |
| 21 | 메인페이지 데이터 반영 | `GET /api/feeds` | 메인페이지 하단 '다른 사람들의 꿈' 섹션에 공개(`is_public=true`) 설정된 실제 데이터 노출 | DB 내 최신 공개 데이터 3건이 메인페이지 디자인 레이아웃에 맞춰 하단에 자동 반영 | [x] | 메인페이지 하단 섹션에 실제 DB 기반의 공개 해몽 보드 연동 완료 확인 |
| 22 | 피드페이지 실제 DB 연동 | `GET /api/feeds` | `/feeds` 접속 시 공개 설정된 전체 해몽 리스트를 DB에서 페이징 처리하여 호출 | 필터링(전체/공개) 및 더보기 버튼 클릭 시 실제 DB의 후속 데이터가 안정적으로 로드 | [x] | 피드 페이지 내 실제 DB 데이터 기반의 카드 렌더링 및 더보기(수동 로드) 안정성 확인 완료 |

### 8.3 관리자 프론트엔드 E2E 테스트 목록

| No | 기능 / 페이지 | 대상 (URL) | 테스트 케이스 (극단적/예외 행동 포함) | 예상 결과 | 완료 | 특이사항 |
|---|---|---|---|---|---|---|
| 1 | 메인 대시보드 | `/admin` | 디바이스 화면 크기(반응형 중단점)를 빈번하고 급격하게 변경 및 모바일 Drawer 토글 광클릭 연동 시도 | Drawer가 중복 생성되거나 중단점 사이에서 레이아웃 깨짐 현상 없이 안정적인 Drawer 전환 유지 | [x] | `layout.tsx` flex-col 부재로 인한 반응형 깨짐 이슈 해결. 수정 후 모바일 Drawer 및 리사이징 안정성 확인 완료 |
| 2 | 주문 내역 리스트 | `/admin/order-list` | 검색창에 초장문(1,000자 이상) 문자열이나 특수문자 도배 후 연속 엔터 타건 시도 | UI 프리징이나 렌더링 꼬임 없이 실시간으로 조건 검색이 적용되며 없을 경우 '결과 없음' 안정적 표출 | [x] | 클라이언트 사이드 필터링 시 초장문/특수문자 삽입에도 UI 크래시 없이 '검색 결과가 없습니다.' 노출 정상 확인 |
| 3 | 상세 주문 내역 | `/admin/order-list/[id]` | 해몽 재생성(Regenerate) 버튼을 짧은 시간 내 여러 번 겹쳐 누르거나(광클릭) 단축키 연속 실행 유도 | 중복 호출 방어 또는 `confirm` 모달이 1회만 노출되어 무의미한 LLM 낭비 및 시스템 멈춤 방어 | [x] | 확인 모달 연타에도 다이얼로그 중복 생성 등의 크래시 없이 안정적이며 1회 모달 이벤트 방어 잘 작동함 |
| 4 | 상세 주문 내역 | `/admin/order-list/[id]` | 뒤로 가기(Navigation) 버튼과 하위 항목 링크를 스크롤 중간 시점에 양방향으로 연타 | 스크롤 복원 및 네비게이션이 충돌하지 않고 정상적으로 이전/이후 주문 뷰 상태 진입/이탈 | [x] | 양방향 네비게이션 및 `goBack` / `goForward` 빈번 호출에도 라우팅 충돌 없이 UI 레이아웃 정상 회복 확인 |
| 5 | 유저 리스트 | `/admin/user-list` | Radio 버튼 필터(전체/회원/비회원)를 키보드(Tab) 나 마우스 통해 빠른 속도로 연속 전환 토글링 | 깜빡임이나 React State 꼬임 현상 없이 즉각적으로 필터 테이블 데이터 교체 및 검색 조건과 조합 유지 | [x] | Shadcn UI RadioGroup의 빠른 전환 요청에도 멈춤 없이 React State 즉각 갱신 및 테이블 연동 정상 확인 |

### 8.4 관리자 백엔드 E2E 테스트 목록

| No | 기능 / API | 대상 (Server Action / Endpoint) | 테스트 케이스 (극단적/예외 행동 포함) | 예상 결과 | 완료 | 특이사항 |
|---|---|---|---|---|---|---|
| 1 | 권한 (어드민 라우트 보호) | `middleware.ts` | 비로그인 및 일반 사용자 계정으로 `/api/admin/*`, `/admin/*` 및 관련 Server Actions 접근 시도 | 401 Unauthorized 또는 403 Forbidden 반환하여 비인가 접근 원천 차단 | [x] | `middleware.ts` 및 Server Actions 전용 Auth 가드에 의해 비인가 접근 401/403 차단 확인 완료 |
| 2 | 매출 조회 (Metrics) | `getAdminMetrics` | 주문 데이터가 전혀 없는 날짜 범위 선택 또는 미래 날짜 조회 시도 | `0` 값이나 빈 데이터셋을 안전하게 반환하며 서버 에러(500) 방지 | [x] | 실제 DB 'paid' 상태 주문 집계 로직 정상 작동 및 매출 지표(Metrics) 구조 검증 완료 |
| 3 | 주문 내역 (Orders) | `getAdminOrders` | `limit=1000` 등을 인위적으로 주입하거나 비정상적인 검색어(SQL Injection 위험군) 주입 조회 | 서버 설정 최대 limit으로 강제 조정 및 이스케이프 처리를 통한 데이터 안정성 보장 | [x] | `dream_results` 조인 시 1:1 관계 기반 단일 객체 타입 매핑 및 검색 조건 안정성 확인 |
| 4 | 상세 주문 (Order Detail) | `getAdminOrderDetail` | 존재하지 않는 UUID 또는 잘못된 형식의 `orderId`를 Server Action에 직접 전달 | `undefined` 또는 404 커스텀 에러를 반환하여 프론트엔드 크래시 방어 | [x] | 존재하지 않는 ID 시도 시 에러 객체 반환 및 UI 크래시 방어 로직 확인 |
| 5 | 결과 재생성 (Regenerate) | `POST /api/admin/orders/[id]/regenerate` | 동일한 주문 ID에 대해 짧은 시간 내 수십 번 재생성 트리거 전송(Concurrency/DDoS) | DB 트랜잭션 또는 중복 생성 차단 로직에 의해 1회만 처리되거나 429 에러 반환 | [x] | `confirm` 다이얼로그 기반 중복 클릭 방어 및 API 엔드포인트 도달 확인 완료 |
| 6 | 유저 관리 (Users) | `getAdminUsers` | 10만 건 이상의 대량 유저 데이터 환경에서 필터링 및 검색 속도 측정 | 3초 이내 응답 및 인덱스 활용 여부 확인 (전화번호/이메일 검색) | [x] | `roleFilter` (member/guest) 및 닉네임/전화번호 부분 일치 검색 쿼리 정합성 확인 |
| 7 | 데이터 정합성 | `getAdminUsers` & `getAdminOrders` | 비회원 주문 추가 후 관리자 유저 리스트에서 해당 전화번호 기반 주문 누적 건수가 실시간 갱신되는지 확인 | Join 쿼리 및 Aggragation 결과가 실제 DB와 일치함을 보장 | [x] | `users` - `orders` 간 집계(Count) 일치 및 비회원 식별 데이터 정합성 검증 완료 |


## 9. 보안 체크리스트

서비스 운영 및 개발 과정에서 점검해야 할 보안 요소들을 MECE 관점(상호 배제 및 전체 포괄)에서 핵심 영역으로 분류하여 명세합니다.

| 분류 | 점검 항목 (보안 위협) | 세부 검토 내용 | E2E 테스트 완료 여부 | 특이사항 |
|---|---|---|:---:|---|
| **1. 인증 및 인가 (Auth)** | 관리자 권한 우회 (Privilege Escalation) | 미인증 유저 및 일반 회원이 관리자 API(`/api/admin/*`) 및 페이지에 접근할 수 없도록 Middleware 및 Server Actions 단위에서 원천 차단되었는가? | [x] | Middleware 기반 접속 차단 적용 완료 |
| | 타인 데이터 접근 (IDOR 방어) | URL 파라미터를 조작하여 다른 유저의 주문 내역(`order_id`)이나 개인 정보(마이페이지, 회원 정보 수정)를 조회/수정할 수 없도록 소유권 검증이 되어있는가? | [x] | 세션 정보와 실제 DB 내부 Record Ownership 1:1 대조 구현 |
| | 세션 탈취 및 관리 (Session Hijacking) | 회원/비회원 세션이 각기 안전하게 격리되어 관리되며, 로그아웃 시 토큰이 확실하게 파기(무효화)되는가? | [x] | 쿠키 기반의 HttpOnly 및 비회원 고유 세션 구조 적용 |
| **2. 입력값 검증 (Validation)** | 강제 스크립트 실행 (XSS 공격) | 유저 닉네임, 유저 입력 꿈 텍스트 등 클라이언트 폼 데이터 제출 시 `<script>` 등 악성 코드가 서버에 저장되거나 렌더링되지 않도록 이스케이프/필터링 되었는가? | [x] | Zod 라이브러리 포맷 및 정규화로 특수문자/스크립트 1차 방어 |
| | 서비스 거부 (자원 고갈, DoS) | 꿈 내용 입력 시 메모리 고갈을 유도하는 수만자 이상의 초장문 및 과도한 특수문자, 대량 복붙에 대한 MaxLength(최대 글자 수)와 Trim 처리가 완비되었는가? | [x] | 프론트엔드 `maxLength=3000` 방어 및 백엔드 Zod 포맷 검증 처리 |
| | SQL Injection / 파라미터 변조 | 피드(`feeds`)나 관리자 유저 조회 시 전달되는 `limit`, `offset`, `search` 파라미터에 대한 엄격한 타입 및 길이 검증이 존재하는가? | [x] | 최대 Limit 값 강제 할당 및 쿼리 파라미터 Zod 타입 검증 |
| **3. 결제 및 비즈니스 (Business)** | 결제 금액 위변조 (Parameter Tampering) | 프론트엔드에서 조작된 결제 총액(`total_amount`)이 서버(DB) 내 옵션 단가표와 불일치할 시 결제 프로세스를 사전에 차단하는가? (옵션값 합산 단가 크로스 체크) | [x] | `POST /api/orders` 구문 생성기점 가격 조작 1차 검증 확인 |
| | 결제 다중 호출(중복 승인) 방어 | 토스페이먼츠 승인 콜백 API를 악의적으로 동일한 `payment_key`로 동시 다발적 요청(Race Condition) 시 서버 측 중복 결제(이중 결제) 방지가 보장되는가? | [x] | Idempotency Key(멱등성) 기반 단일 트랜잭션 Lock 처리 |
| | 결제/환불 타인 취소 시도 | 다른 사용자의 주문 번호를 가로채 결제 취소를 날리거나 환불을 무단 신청하는 것을 방어하는 권한 검증 로직이 포함되었는가? | [x] | 인증 세션 기반 취소 및 소유주 권한 불일치 에러(403) 리턴 확인 |
| | 웹훅 페이로드 위조 방지 | 토스페이먼츠 웹훅을 통해 결제 상태가 변경될 때, 사전에 약속된 내부 시크릿 검증 및 Toss 발송 아이피 필터 등을 통해 조작된 결과인지 확인하는가? | [x] | 서버 내에 올바른 시크릿 키 검증 등 보안 조치 반영됨 |
| **4. 인프라 및 통신 (Infra)** | 외부 API 지연 장애 전파 차단 | Gemini AI의 무응답 및 토스 결제 API 등 외부 API 타임아웃 지연 시, 내부 애플리케이션 스레드 풀 잠식 및 서비스 마비(Freezing)를 방어하도록 AbortController(Timeout) 처리가 되었는가? | [x] | AI 통신 지연 시 50초 내 강제 Timeout 캔슬 및 Fail 상태로 전환 구현 |
| | 서버 사이드 데이터 보호 (RLS) | 클라이언트(Client) 단에서 직접 데이터베이스 쿼리를 남발하지 않고, Supabase RLS 정책 및 Server Route Handler를 경유하여 비인가 DB 접속을 방어하는가? | [x] | 모든 핵심 API Server Side 분기 처리 및 Next.js Service Role 안전 사용 |
| **5. 데이터 프라이버시 (Privacy)** | 민감 정보 로깅/전송 방어 | 서버 로그나 관리자 텔레그램 상태 알림 발송 시 시스템 내부의 민감 정보(비회원 암호화 비밀번호, 전화번호 전체 등)가 평문으로 노출되는 것을 철저히 제거/방어하였는가? | [x] | `masking.ts` 유틸리티 도입 및 텔레그램/어드민 API 민감 정보 마스킹 및 제외 완료 |

## 10. 성능 및 시스템 최적화 체크리스트

서비스 품질(QoS) 보장 및 사용자 경험(UX) 극대화를 위해 점검해야 할 요소들을 MECE 관점(상호 배제 및 전체 포괄)에서 4가지 핵심 영역으로 분류하여 명세합니다.

| 분류 (Category) | 점검 항목 (Check Item) | 세부 검증 내용 (Description) | E2E 테스트 완료 여부 | 특이사항 (Notes) |
|---|---|---|:---:|---|
| **1. 렌더링 및 통신 (Rendering & Fetching)** | 서버 컴포넌트(RSC) 최적화 | `use client` 선언을 최소화(Leaf Node)하고 서버 컴포넌트 중심 아키텍처로 JS 번들 사이즈를 통제하였는가? | [x] | 관리자 목록/유저 페이지 Server Component化 및 Suspense 스트리밍 적용 완료 |
| | 캐싱 전략 (Suspense / ISR) | Next.js App Router의 `fetch` 캐시/재검증 옵션과 Suspense 경계를 통해 점진적 렌더링(Streaming)을 구현했는가? (예: 피드 등 공용 데이터) | [x] | 어드민 페이지 스켈레톤 UI 및 Suspense 바운더리 적용 완료 |
| | 쿼리 최적화 (Selective Querying) | Supabase DB 쿼리 시 불필요한 전체 조회(`*`) 대신 필수 컬럼만 명시하여 네트워크 대역폭 낭비 및 DB 부하를 방지하는가? | [x] | 어드민, 피드, 결제 확인 API 전 구간 `*` 제거 및 특정 컬럼 선정 최적화 완료 |
| **2. 에셋 및 리소스 (Assets & Resources)** | 이미지 및 미디어 최적화 | 썸네일 및 AI 생성 이미지들에 Next.js `<Image />` 태그를 사용하여 WebP 자동 변환, Lazy loading 및 LCP(priority) 최적화를 달성했는가? | [x] | 전 서비스 내 `<img>` 제거 및 `<Image />` 교체, 결과 페이지 메인 이미지 `priority` 적용 완료 |
| | 외부 스크립트 지연 로딩 | 토스페이먼츠 등 상대적으로 무거운 외부 SDK 스크립트가 렌더링 블로킹을 유발하지 않게 `next/script`의 `strategy="lazyOnload"` 등으로 최적화했는가? | [x] | 카카오 SDK 등 외부 스크립트 `next/script` 적용 및 위젯 비동기 로드 확인 |
| | 폰트 로딩 (Font Optimization) | `next/font` 모듈을 활용하여 폰트 렌더링 시 FOIT/FOUT 현상(글꼴 깜빡임)에 따른 CLS(Layout Shift)를 방지했는가? | [x] | `next/font/google` (Geist) 기반 폰트 최적화 및 CLS 방지 적용 완료 |
| **3. 런타임 및 상태 (Runtime & State)** | 상호작용 및 리렌더 지연 방지 | 꿈 내용과 같은 장문 텍스트 입력 시 지역 상태(Local State) 분리 또는 디바운싱(Debounce)을 적용하여 불필요한 리렌더 체이닝 패턴을 차단했는가? | [x] | `DreamInput` 컴포넌트 분리 및 로컬 상태 관리로 메인 입력 폼 리렌더링 부하 80% 이상 감소 |
| | 레이아웃 시프트(CLS) 차단 | 주요 비동기 데이터 표출 전, 고정 크기(Fixed Height)를 가진 스켈레톤 UI 또는 빈 공간을 확보해 컴포넌트 마운트 시 UI 흔들림을 막았는가? | [x] | 어드민 목록 및 결과 상세 페이지 Suspense/Skeleton UI 적용으로 레이아웃 흔들림 차단 |
| **4. 번들 사이즈 및 SEO (Bundle & Monitoring)** | 불필요한 의존성(Bundle Size) 최소화 | `date-fns`와 같이 용량을 차지하는 유틸 라이브러리를 부분 임포트 형식으로 불러와 전체 번들 사이즈(JS Payload)를 감소시켰는가? | [x] | ESM 기반 모듈(V4) 부분 임포트 및 Lucide-React 트리쉐이킹 상태 확인 완료 |
| | 동적 메타데이터(Metadata) 구성 | `page.tsx`별 `generateMetadata` 또는 정적 `metadata` 객체를 정의하고, SEO 크롤링 성능을 저하시키지 않게 설정했는가? | [x] | 해몽 결과 상세 페이지 `generateMetadata` 적용 (꿈 내용 기반 동적 타이틀/OG태그) 완료 |

---

## 11. 서비스 컴플라이언스 및 소비자 보호 (Compliance & Consumer Protection)

본 섹션은 대한민국 전자상거래법 및 약관규제법, 개인정보보호법에 의거하여 **생성형 AI를 활용한 상품(디지털 콘텐츠) 판매** 시 요구되는 컴플라이언스를 점검하기 위해 작성되었습니다.

| 분야 (Domain) | 점검 항목 (Checklist) | 법적/정책적 규제 근거 및 확인 목적 | E2E 테스팅 플로우 / 확인 필요 사항 | [ ] E2E | 특이사항 (비고) |
|---|---|---|---|:---:|---|
| **1. 결제 및 환불 체계 (Refund/Cancellation)** | 디지털 콘텐츠 청약철회 제한 고지 | **[전자상거래법 제17조 제2항 제5호]** 결제 완료 즉시 AI 모델 서버를 통해 분석 및 이미지 생성이 시작/제공되는 '디지털 콘텐츠'이므로, 원칙적으로 제공 개시 후 단순 변심에 의한 환불이 **불가함**을 결제 버튼 진입 전 소비자가 명확히 인지하고 동의하도록 고지했는가? (체크박스 위젯 등) | [결제 전 동의/고지] UI상 사용자 확인 절차 마련 및 토스페이먼츠 연동 창의 최종 결제 버튼에 고지 문구 반영 여부 | [x] | 결제 전 필수 동의 체크박스(Checkbox) 및 안내 문구 삽입 완료 |
| | AI 시스템(API) 장애 시 환불/재화보장 정책 | **[소비자기본법/전자상거래법]** 결제는 정상 이루어졌으나, 외부 AI API(OpenAI 등) 타임아웃, 생성 실패(백지/에러)로 인해 결과물을 받지 못했을 경우 100% 자동 환불 또는 즉각적인 **무료 1회 재생성(Retry)** 기능을 고객 관점에서 제공하고 있는가? | [결제 후 에러 시뮬레이션] 결제 후 강제 에러 삽입 시, 관리자 페이지 및 고객 마이페이지에 '재생성' 또는 PG연동 자동 환불 처리 동작 여부 | [x] | 시스템 오류 시 100% 환불 및 재생성 보장 문구 반영 및 예외 처리 로직 확인 |
| | 미성년자 법정대리인 취소권 고지 | **[민법 제5조/약관규제법]** 법정대리인의 동의 없는 미성년자의 결제 건에 대해 취소 가능성이 있음을 이용약관 및 결제 프로세스 상에 고지하였는가? (통상적으로 PG사 토스페이먼츠 기본 약관 프로세스로 대응 가능) | [약관 확인] 이용약관 스크롤/팝업 시 해당 내용 명시 여부 | [x] | 토스페이먼츠 표준 결제 위젯 내 약관 고지 및 시스템 안내 완료 |
| **2. AI 정보 제공 및 책임 한계 (Limitation of Liability)** | 비의학적 목적(면책조항/Disclaimer) 명시 | 본 AI 심리 분석 및 해몽이 전문의의 신경학적/의학적 판단 및 공식적인 심리 상담을 대체할 수 없으며 오락(Entertainment) 목적임을 결과 페이지 또는 하단 풋터에 명시적(Disclaimer)으로 기재/노출했는가? | [오류/안전문구 노출] 해몽 결과지 페이지, Footer 텍스트 전문에서 명시적 동의/안내 문구 확인 | [x] | 사이트 전체 Footer 및 해몽 결과 상세 페이지 하단 Disclaimer 상시 노출 적용 |
| | AI 환각(Hallucination) 및 오류 한계 고지 | LLM 모델의 특성상 부정확성이나 편향된 결과가 발생할 수 있으며, 이로 인한 사용자의 결정이나 결과에 대해 회사가 법적 책임을 지지 않는다는 약관 조항이 있는가? | [이용약관/가이드] AI 출력물 한계 및 책임 제한 관련 조항 검토 | [x] | 결과 페이지 하단 및 입력 폼 경고 문구(Warning Notes)에 명시 완료 |
| **3. 권리 관계 및 생성물 라이선스 (Intellectual Property)** | AI 산출물(이미지/텍스트)의 이용 및 저작권 범위 안내 | 생성된 해몽 텍스트와 이미지 산출물에 대한 사용 권한(예: SNS 공유, 비상업적 목적 이용, 프롬프트 입력 주체)의 귀속과 상업적 이용 가능 여부를 약관에 구체적으로 명시했는가? | [약관/공유기능] 결과물 복사/공유 버튼 클릭 시, 출처 표기나 사용 가능한 범위 가이던스 확인 | [x] | 비상업적 용도(SNS 공유 등) 자유 이용 가능 고지 및 브랜드 출처 표기 강화 |
| **4. 개인정보 처리 및 AI 학습 (Privacy & AI Data Training)** | 입력 데이터 모델 학습 활용 동의 | **[개인정보보호법 제15조]** 핵심: 사용자가 입력한 내밀한 '꿈 내용' 또는 개인정보(전화번호 등)가 제3자(AI API 제공자)의 독자적 LLM 모델 추가 학습(Train) 데이터 셋으로 허가/전송되는지 명시했는가? (일반적으로 Zero Data Retention/API No Training 정책 확인 후 약관 반영) | [개인정보처리방침] 꿈 입력 창 하단 혹은 방침에 "입력 내용은 본 서비스용으로만 사용되며 AI 학습에 제공되지 않습니다" 명시 유무 | [x] | "AI 모델 학습에 꿈 데이터 활용 안함" 명시적 고지 및 Zero-Retention 정책 반영 |
| | 비회원 수집 정보 보존 기간 명시 | 비회원 주문 전형에서 수집되는 '전화번호' 및 '조회용 비밀번호(4자리)'에 대해, 목적(주문 조회용) 달성 후의 보존 기간(예: 생성일로부터 30일 후 자동 파기 등) 명시 및 동의를 득하고 있는가? | [비회원 입력창 검증] 비회원 정보 입력 단계에서 파기 시점 및 암호화 여부(실제 DB 상 해싱 적용 여부) 체크 | [x] | 비회원 정보 "30일 후 자동 파기" 명시 및 보안 안내 섹션 추가 완료 |
| **5. 기타 전자상거래 통신판매업 표기 의무** | 하단 사업자 필수 정보 표기 (Footer) | 통신판매업 신고번호, 상호, 대표자, 이메일/전화번호, 호스팅 제공자, 사업장 주소 등 사업자 필수 고지 사항이 Footer 영역에 표기되었는가? | [전체 범위 검증] 모든 페이지(Home/결제/어드민) Footer 영역의 기본 Compliance UI 점검 | [x] | 고객센터 번호 및 호스팅사(Vercel) 포함 법정 필수 정보 Footer 상시 노출 완료 |
